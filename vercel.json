{
  "buildCommand": "cd _client && npm ci && npm run build",
  "outputDirectory": "_client/dist",
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/index" }],
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "memory": 1024
    }
  }
}
